"$schema" = "schemas/schema.json"

[default]
description = "Default permissions for the Android keystore plugin — allows set/get/delete of encrypted preferences."
permissions = ["allow-set", "allow-get", "allow-delete"]
